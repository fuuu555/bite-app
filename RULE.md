# BiteMap 開發規則

## 需求與文件一致性

1. 每次開發前，先閱讀 `FUNCTIONAL_REQUIREMENTS.md`、`DEVELOPMENT_PLAN.md` 與相關現有程式。
2. 如果預計功能與上述文件不同、擴充文件未提到的行為，或改變既有驗收條件，必須先向使用者說明差異並取得確認。
3. 使用者確認後，才開始實作；實作完成後，必須把已確認的新增或修改同步補回 `FUNCTIONAL_REQUIREMENTS.md` 或 `DEVELOPMENT_PLAN.md`。
4. README 只補充操作方式、技術架構與管理後台說明，不取代功能需求文件。
5. 如果只是修正錯誤、補測試、調整樣式或完成文件已明確規劃的工作，可直接處理，但仍要更新必要的測試與說明。

## 資料與權限

- 關聯式資料庫維持第三正規化（3NF）；可獨立查詢的業務事實使用關聯表，不用 JSON 或重複欄位取代。
- 一般使用者不能靠前端隱藏按鈕取得管理權限；所有管理 API 都要在後端檢查角色。
- `merchant` 角色目前只預留，不得未經確認就開放商家編輯流程。
- 外部服務、地圖與爬蟲功能先確認授權與保存範圍，再加入正式資料流程。

## 程式品質

- 新增或修改的程式碼使用中英文註解說明重要邏輯。
- 修改資料庫模型時，必須新增可逆的 Alembic migration。
- 修改 API 後同步產生 OpenAPI client 型別。
- 完成後至少執行相關的 formatter、lint、typecheck 與測試。
- 不把 `.env`、密碼、Token 或其他秘密加入 Git。
