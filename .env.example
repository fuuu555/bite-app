# Local development defaults only. Replace secrets in deployment environments.
APP_ENVIRONMENT=development
DATABASE_URL=postgresql+asyncpg://bitemap:bitemap_local@127.0.0.1:5433/bitemap
API_CORS_ORIGINS=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:8000

POSTGRES_DB=bitemap
POSTGRES_USER=bitemap
POSTGRES_PASSWORD=bitemap_local
