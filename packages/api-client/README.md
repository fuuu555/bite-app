# BiteMap API Client

This package stores TypeScript types generated from the FastAPI OpenAPI document.
此套件保存由 FastAPI OpenAPI 文件產生的 TypeScript 型別。

Start the backend first, then run:

```powershell
pnpm openapi:generate
```
