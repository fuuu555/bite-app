/**
 * Shared generated API types / 共用的 API 產生型別。
 *
 * Run `pnpm openapi:generate` while the backend is running to refresh schema.d.ts.
 * 後端啟動後執行 `pnpm openapi:generate` 更新 schema.d.ts。
 */
export type { paths } from "./schema";
