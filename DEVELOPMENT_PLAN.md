# BiteMap 開發計畫

本計畫依據 [`FUNCTIONAL_REQUIREMENTS.md`](./FUNCTIONAL_REQUIREMENTS.md) 與 [`README.MD`](./README.MD) 制定。

## 1. 開發策略

採用「功能垂直切片」，不先完成全部前端，也不先完成全部後端。每個階段只建立該功能真正需要的前端、API、資料表與測試，階段結束時必須產生可以操作及驗收的成果。

資料庫 schema 持續遵守第三正規化（3NF）：每個非鍵欄位只依賴該表的鍵，不以 JSON 或重複欄位保存可獨立查詢的業務事實；例外僅限衍生欄位、外部原始回應或已有明確查詢與一致性理由的非正規化資料，且必須另行記錄理由。

身分與權限先共用同一套 `users` 身分基礎，但登入入口與授權責任分開：

| 角色       | 登入入口       | 目前權限                                     |
| ---------- | -------------- | -------------------------------------------- |
| `user`     | `/login`       | 公開地圖、一般使用者功能；不可新增或編輯商家 |
| `admin`    | `/admin/login` | 管理料理分類與店家新增、編輯、發布、封存     |
| `merchant` | 預留           | 預留給店家帳號；目前不開放商家編輯流程       |

前端隱藏管理按鈕不算授權；所有管理 API 都必須在後端檢查 `admin` 角色。未來啟用 `merchant` 時，仍需額外建立商家帳號與店家的關聯及審核規則，不能讓一般使用者直接取得編輯權限。

第一條產品主線為：

```text
專案基礎
  ↓
管理員新增店家
  ↓
公開地圖查詢店家
  ↓
料理標記、群聚與預覽卡
  ↓
探索、搜尋與餐廳詳細頁
```

地圖適合作為第一個產品功能，因為它能同時驗證：

- Next.js 與 FastAPI 的 API 合約。
- PostgreSQL／PostGIS 空間資料。
- MapLibre GL JS 與 OpenFreeMap 前端整合。
- 管理員店家資料流程。
- Mobile First 互動方式。
- 後續探索、約飯及評論共用的餐廳主資料。

但第一個程式階段仍是最小專案基礎，不能直接從地圖畫面開始，否則店家來源、API 與測試環境都會缺少依據。

## 2. 階段總覽

| 階段                  | 可驗收成果                                 | 主要依賴      |
| --------------------- | ------------------------------------------ | ------------- |
| 0. 專案基礎           | Web、API 與 PostGIS 可在本機啟動           | 無            |
| 1. 店家管理           | 管理員可以新增、定位、編輯及發布店家       | 階段 0        |
| 2. 公開地圖 MVP       | 使用者能定位、搜尋可視區域及查看店家       | 階段 1        |
| 3. 地圖完整體驗       | 群聚、搜尋、料理顏色、篩選及手機預覽卡可用 | 階段 2        |
| 4. 探索與餐廳頁       | 搜尋、Top 3、完整列表及餐廳詳細頁可用      | 階段 3        |
| 5. 使用者帳號與個人頁 | 一般使用者可以註冊、登入及管理基本資料     | 階段 0        |
| 6. 評價、再訪與收藏   | 餐廳內容形成完整使用者回饋流程             | 階段 4、5     |
| 7. 約飯與投票         | 公開／私人約飯與候選餐廳投票可用           | 階段 4、5     |
| 8. 社交與聊天室       | 好友、追蹤、私訊及約飯聊天室可用           | 階段 5、7     |
| 9. PWA 與正式環境     | 可安裝、可部署、可監控的 Web App           | 前述 MVP 功能 |
| 10. 店家資料自動補充  | 區域需求、候選資料及管理審核流程可用       | 階段 1～3、9  |

階段 4 與階段 5 在人力充足時可以平行；其餘階段以表格中的依賴順序進行。

## 3. 階段 0：專案與開發環境基礎

### 目標

建立最小、可重現、可測試的開發骨架。不在此階段提前建立尚未使用的 Celery、聊天室或推薦系統。

### 工作內容

#### 專案結構

- 初始化 Git repository，加入適合 Node、Python、環境檔及本機工具的 `.gitignore`。
- 建立 `apps/frontend`：Next.js App Router、React、TypeScript。
- 建立 `apps/backend`：FastAPI、Pydantic、SQLAlchemy 2、Alembic。
- 建立 `packages/api-client`：保留給 OpenAPI 產生的 TypeScript Client。
- 建立 `tests/e2e`：Playwright 測試入口。

#### 本機服務

- 使用 Docker Compose 啟動 PostgreSQL + PostGIS。
- Redis 先保留於架構文件，不在沒有實際功能使用時強制啟動。
- 建立 `.env.example`，只列變數名稱及安全說明，不放真實憑證。
- 建立資料庫連線與第一個 Alembic migration。

#### 最小功能

- Web 顯示 BiteMap 開發首頁。
- API 提供 `/health/live` 與 `/health/ready`。
- Readiness 檢查資料庫連線與 PostGIS extension。
- 產生 OpenAPI 規格並建立前端 Client 產生指令。

#### 品質基線

- Web：ESLint、Prettier、TypeScript strict mode、Vitest。
- API：Ruff、Pyright、Pytest。
- Commit 前可執行格式、型別與單元測試。
- CI 執行 Web 與 API 的安裝、格式檢查、型別檢查及測試。

### 完成條件

- 新環境依 README 指令可以啟動 Web、API 與 PostGIS。
- Web 能呼叫 API health endpoint。
- Alembic migration 可以從空資料庫成功升級及回復一個版本。
- 前後端測試骨架與 CI 均通過。

## 4. 階段 1：店家主資料與管理員新增店家

### 目標

先建立地圖的可靠資料來源。階段結束時，管理員能在後台建立一間可公開顯示的店家。

### 資料庫

先建立地圖與探索共用的最小資料模型：

- `users`：支援 `user`、`admin` 與預留的 `merchant` 角色；Stage 1 只建立管理員登入。
- `cuisines`：料理分類、顯示名稱、色票及圖示鍵值。
- `restaurants`：名稱、地址、可選的 `menu_url`、主要料理分類、價格區間、發布狀態、來源類型。
- `restaurants.location`：`geography(Point, 4326)` 與 GiST index。
- `audit_logs`：店家建立、座標修改、發布與封存事件。
- `audit_log_changes`：每次稽核事件的欄位級前後值，以事件 ID 與欄位名稱作複合主鍵，避免 JSON 快照破壞 3NF。

店家狀態先支援：

- `draft`
- `published`
- `archived`

MVP 不建立外部候選店家資料表，留到階段 10 才加入。

### 後端

- 建立管理員登入與角色授權的最小完整流程；一般使用者登入在階段 5 實作，`merchant` 僅先保留角色欄位。
- 建立料理分類 CRUD。
- 建立店家新增、列表、查看、修改、發布及封存 API。
- 店家表單先支援一個可選的 `menu_url`；圖片、PDF 與菜單品項保留後續調整，不在此階段固定資料模型。
- 在資料庫與 Service 層驗證主要料理分類、價格與座標。
- 建立 `GeocodingProvider` 介面；Stage 1 先以可替換的 Nominatim adapter 支援地址定位與反向定位，正式環境需依服務條款與流量需求確認 Provider。
- 管理 API 與公開 API 分離，不讓前端隱藏按鈕取代後端授權。

開始地址自動定位前，必須先選定一個授權允許目前使用方式的 Geocoding Provider；若尚未選定，可先完成地圖點選與圖釘拖曳，但不能把地址自動定位標示為完成。

建議端點：

```text
POST   /api/v1/admin/session
GET    /api/v1/admin/cuisines
POST   /api/v1/admin/cuisines
GET    /api/v1/admin/restaurants
POST   /api/v1/admin/restaurants
GET    /api/v1/admin/restaurants/{id}
PATCH  /api/v1/admin/restaurants/{id}
POST   /api/v1/admin/restaurants/{id}/publish
POST   /api/v1/admin/restaurants/{id}/archive
POST   /api/v1/admin/geocode
```

### 前端

- 建立 `/admin/login`。
- 建立料理分類管理頁，設定固定色票與圖示。
- 料理分類支援預設色票、自訂顏色與後台圖示預覽，也可停用／啟用或刪除未被店家使用的分類；店家支援封存、解封與永久刪除；桌面版分類與店家清單每頁最多顯示 8 筆並提供分頁；`display_name` 用於人類閱讀，`slug` 用於穩定的系統識別，並由顯示名稱自動產生，必要時可在進階設定手動調整。
- 建立店家列表、新增及編輯頁。
- 新增及編輯頁可輸入、修改及查看菜單網址。
- 地址輸入後顯示定位候選結果；地圖點擊或拖曳後將反向定位結果同步回地址欄位。
- 允許直接點擊地圖建立圖釘。
- 地址定位與地圖點選都產生可拖曳圖釘。
- 新增店家地圖採純 2D 底圖，不顯示 3D 建築、店家標記或群聚。
- 儲存前顯示地址、座標、主要料理、價格與發布狀態確認。

### 測試

- 非管理員不能呼叫管理 API。
- 缺少座標、主要料理或價格時不能發布。
- 座標能正確寫入及讀回 PostGIS。
- 地址定位失敗時仍能改用地圖點選。
- 拖曳圖釘後保存的是更新後座標。
- 發布與封存操作會產生 audit log。

### 完成條件

- 管理員不用直接操作資料庫即可建立及發布店家。
- 已發布店家具有有效座標、主要料理分類及價格。
- 一般使用者無法取得管理 API 權限。
- 可以建立足夠的測試店家資料供地圖開發使用。

## 5. 階段 2：公開地圖 MVP

### 目標

完成第一個使用者可操作的產品頁面。先驗證定位、可視範圍查詢與單店互動，群聚細節留到下一階段。

### 後端

- 建立公開地圖查詢 Service。
- 公開地圖只顯示資料庫中狀態為 `published` 的店家。
- 只查詢 `published` 店家。
- 使用目前可視範圍查詢 PostGIS，禁止無邊界取得全部店家。
- 支援料理分類與價格參數，但此階段可以先完成 API，再於階段 3 加入完整 UI。
- 設定合理的結果上限；超過時回傳可辨識的 `zoom_required` 狀態。

建議端點：

```text
GET /api/v1/map/restaurants
    ?west={lng}
    &south={lat}
    &east={lng}
    &north={lat}
    &zoom={level}
    &cuisine_ids={ids}
    &price_ranges={ranges}
```

最小回傳欄位：

- 店家 ID
- 名稱
- 經緯度
- 主要料理分類與顏色鍵值
- 價格區間
- 預覽圖片 URL（有資料時）

### 前端

- 建立 Footer 與五個主入口，地圖為第二個入口。
- 建立 MapLibre GL JS 載入層與 OpenFreeMap style 設定，避免不同頁面重複初始化地圖。
- 進入地圖時依序使用目前位置、上次位置、預設城市。
- 未授權定位時仍正常顯示地圖。
- 拖曳或縮放後顯示「搜尋此區域」。
- 點擊按鈕後才使用目前 bounds 查詢 API。
- 查詢期間保留舊標記並顯示載入狀態。
- 單店標記使用主要料理分類顏色。
- 點擊標記開啟底部預覽卡，再由卡片進入暫時的餐廳頁路由。

### 錯誤處理

- 定位拒絕：使用上次位置或預設城市。
- MapLibre／OpenFreeMap 載入失敗：顯示重試與可理解的錯誤訊息。
- API 失敗：保留舊結果並提供重試。
- 查無店家：顯示空狀態，不將地圖重設到其他位置。
- 結果過多：要求使用者放大地圖後重新搜尋。

### 測試

- PostGIS 邊界內外店家查詢。
- 僅回傳 `published` 店家。
- 定位成功、拒絕及逾時三種流程。
- 移動地圖不會自動呼叫搜尋 API。
- 點擊「搜尋此區域」使用最新 bounds。
- API 失敗後原有標記仍存在。
- 點擊標記能開啟正確店家的預覽卡。

### 完成條件

- 手機及桌面瀏覽器都能查看附近已發布店家。
- 搜尋範圍、錯誤狀態及定位降級符合需求文件。
- 地圖 API 有整合測試，主要使用流程有 Playwright E2E 測試。

## 6. 階段 3：群聚、篩選與完整地圖體驗

### 目標

完成參考圖中的縮放效果，使地圖成為可以正式展示的第一個核心功能。

### 搜尋與篩選

- 建立地圖上方搜尋列，支援地區／地址與已發布店家名稱。
- 搜尋結果分成 Geocoding Provider 的地區結果與 BiteMap `published` 店家結果；兩者不可混為外部店家資料。
- 店家結果最多顯示 5 筆；點擊店家後移動地圖、放大並開啟預覽卡，點擊地區後移動地圖並顯示「搜尋此區域」。
- 篩選面板預設收起，展開後提供縣市／行政區、價格與料理分類。
- 篩選先暫存，按「套用篩選」後收起並保留條件標籤；只有按「搜尋此區域」才查詢 API。
- 擴充公開地圖 API 的 `city`、`district`、`cuisine_ids` 與 `price_ranges` 條件。
- 初期地區條件使用標準化地址文字的 allowlisted 關鍵字比對，不新增重複行政區欄位；若未來地址品質或資料量不足，再提出獨立地址／行政區資料模型與 migration。

### 前端

- 使用 MapLibre GeoJSON source clustering。
- 自訂群聚 Renderer：100 家以下顯示精確數量，超過顯示 `100+`。
- 點擊群聚後平滑放大至群聚範圍並重新拆分。
- 確保放大到單店後顯示料理分類標記。
- 加入料理分類與價格篩選控制。
- 加入地區／地址與店家名稱搜尋列及分組下拉結果。
- 桌面版篩選面板從搜尋列下方展開；手機版面板可垂直捲動，條件標籤可水平滑動。
- 篩選變更後明確提示需要重新「搜尋此區域」。
- 加入料理分類圖例。
- 顏色搭配圖示或文字，不讓色覺差異使用者只能依顏色辨識。
- 完成手機底部預覽卡的拖曳、關閉及鍵盤操作。

### 後端與效能

- 驗證 cuisine 與 price 查詢參數。
- 建立店家名稱搜尋與地區 Geocoding 結果的分離 service。
- 驗證 city／district 與店家名稱搜尋只回傳可公開資料。
- 確認空間索引被查詢計畫使用。
- 記錄查詢範圍、結果數量、執行時間及 `zoom_required` 次數。
- 對相同 bounds bucket 與篩選條件保留短時間快取介面；資料量未達需求前不急著加入 Redis。
- 定義轉換為後端群聚的監控指標，但 MVP 不實作後端群聚或向量圖磚。

### 測試

- 群聚數字 `1`、`100`、`101` 的顯示規則。
- 群聚點擊後 zoom 與 bounds 正確改變。
- 地區與店家搜尋候選的分組、上限與選取後地圖移動。
- 多料理店家只使用主要料理顏色。
- 地區、料理與價格組合篩選，且套用前不觸發地圖查詢。
- 地圖鍵盤操作、可讀標籤及色彩對比。
- 手機寬度下標記、Footer、篩選與底部卡不互相遮擋。

### 完成條件

- 能完整展示「遠距群聚 → 中距拆群 → 近距店家標記」。
- 群聚數字、搜尋、料理顏色、篩選及預覽卡符合確認後的需求。
- 這一階段完成後，才進入探索功能；不要同時擴張到評論或約飯。

## 7. 階段 4：探索、搜尋與餐廳詳細頁

### 目標

讓地圖與探索共用同一份餐廳資料，完成匿名使用者從找店到查看詳細資訊的流程。

### 開發內容

- PostgreSQL `pg_trgm`／Full Text Search 餐廳名稱搜尋。
- 距離、價格及料理分類篩選。
- 可測試的綜合排序 Service。
- Top 3 與完整搜尋結果使用相同排序來源。
- 排序切換：距離、價格、App 再訪率、Google 評價、評論可信度。
- 餐廳詳細頁第一層與第二層資訊。
- Google 資料與 App 資料分開顯示。
- 菜單、餐點照片與最後更新時間的基礎資料模型。
- 收藏、加入約飯與留言操作先建立明確入口；只有已完成的功能才啟用。

### 完成條件

- 使用者可以由搜尋或地圖進入同一餐廳詳細頁。
- Top 3、完整列表及排序切換結果可重現並有測試。
- 不建立未經產品決策的黑箱分數。

## 8. 階段 5：一般使用者帳號與個人頁

### 目標

在需要使用者內容前完成正式的身分與 Session 基礎。

### 開發內容

- 註冊、登入、登出、Session 更新與裝置登出。
- Secure、HttpOnly、SameSite Cookie。
- Argon2id 密碼雜湊與登入限流。
- 個人資料、頭像、名稱與 Tag。
- 公開個人頁與自己的設定頁分離。
- 權限 Policy 基礎，供評論、約飯、聊天室及封鎖共用。

### 完成條件

- 使用者能安全登入並維持 Session。
- 公開資料與只有本人可見的資料有 API 測試。
- 管理員、一般使用者與預留的 `merchant` 沿用同一套身分基礎，不建立第二套登入系統；管理後台仍使用獨立 `/admin/login` 入口與後端角色授權。

## 9. 階段 6：留言、評價、再訪與收藏

### 目標

完成餐廳頁最重要的 App 原生內容與再訪率資料來源。

### 開發內容

- 留言文字必填、照片可選。
- 留言完成後選擇會再訪／普通／不會。
- 可選原因標籤。
- 再訪新增時間線，不覆蓋舊紀錄。
- 統計只採同一使用者的最新狀態。
- 留言按讚與綜合／最新／熱門／狀態篩選。
- 收藏餐廳與個人收藏列表。
- S3 相容 Object Storage、簽名上傳及圖片驗證。
- Celery 在此階段加入，先用於圖片工作及可重試通知；Redis 同時加入作為 Broker。

### 完成條件

- 一名使用者對一家店永遠只佔一票，但能查看完整再訪紀錄。
- 再訪率與狀態比例能反映最新紀錄。
- 小樣本數會明確顯示，不隱藏或誇大結果。

## 10. 階段 7：約飯與投票

### 目標

完成餐廳與多人決策的主要閉環，聊天室先留到下一階段。

### 開發內容

- 公開與私人約飯資料模型。
- 成員、申請、審核、退出與移除狀態。
- 最多三家候選餐廳。
- 每位正式成員一票。
- 平票時只在平手候選中隨機選出結果。
- 餐廳詳細頁「加入約飯」建立候選 1。
- 額滿、截止及發起人提前開始投票。
- 約飯權限 Service，供投票與聊天室共用。

### 等待產品決策

以下規則未確認前只保留擴充位置，不自行實作：

- 私人約飯正式條件。
- 私人約飯是否完全沿用公開投票流程。
- 精確截止時間預設值。

### 完成條件

- 公開約飯可以建立、加入、選候選餐廳、投票及產生結果。
- 私人約飯能申請與審核，但未決定規則不會被程式自行補齊。
- 被移除的成員立刻失去投票權限。

## 11. 階段 8：社交安全與聊天室

### 目標

在已存在使用者及約飯成員資格後，加入即時溝通與跨功能社交規則。

### 開發順序

1. 封鎖與跨功能 Policy。
2. 追蹤。
3. 好友邀請、接受與解除。
4. 私訊與陌生人首則訊息限制。
5. 好友聊天沿用原私訊紀錄。
6. 約飯群組聊天室及成員權限。

### 技術內容

- PostgreSQL 永久保存 Conversation、Member 與 Message。
- FastAPI WebSocket 處理連線、驗證及事件。
- Redis Pub/Sub 協調多個 API instance。
- REST API 處理歷史訊息分頁與聊天列表。
- WebSocket 只推送事件，不作訊息唯一儲存位置。
- 被移除或退出約飯後立即取消聊天室讀寫權限。

### 等待產品決策

同飯局成員互相封鎖後的留席與聊天室規則尚未確定。在決策前，不自行讓任何一方退出飯局。

### 完成條件

- 重新連線後不遺失訊息。
- 非成員無法查看或訂閱約飯聊天室。
- 封鎖規則在私訊、好友、追蹤、個人頁與約飯入口一致生效。

## 12. 階段 9：PWA、部署與正式環境強化

### 目標

把已驗證的 Web MVP 轉為可安裝、可觀測、可安全營運的版本。

### 開發內容

- Web App Manifest、圖示、standalone 顯示模式。
- Service Worker 先快取 App Shell 與安全的靜態資源。
- Service Worker 更新提示與版本失效策略。
- Web Push 權限、訂閱及通知偏好。
- Next.js、FastAPI、Celery Worker 分開部署。
- PostgreSQL、Redis、Object Storage 使用代管服務與備份。
- OpenTelemetry Trace、Sentry、結構化 Log。
- API、地圖外部服務及背景工作的費用與配額監控。
- Rate limit、檔案上傳限制、Secret 管理與安全標頭。
- 正式環境 Migration、回滾及資料備份演練。

### 完成條件

- 支援的手機瀏覽器可以安裝 PWA。
- Service Worker 更新不會讓使用者長期停留在舊版。
- 主要 E2E 流程在預備環境通過。
- 錯誤可追蹤至 Web、API 或 Worker 的同一 Trace。

## 13. 階段 10：未來店家資料自動補充

### 啟動條件

只有在手動店家維護成本已成為問題，且已確認合法、允許保存的資料來源後才開始本階段。

### 開發內容

- 建立區域 Coverage Service。
- 記錄「搜尋此區域」的範圍、次數、結果數與最後蒐集時間。
- 定義資料不足門檻與重複排程冷卻時間。
- 使用 Celery `area_discovery` 工作執行背景蒐集。
- 每個外部來源實作獨立 Provider Adapter。
- 建立 `restaurant_candidates`，與正式 `restaurants` 分開。
- 候選資料正規化與去重。
- 管理員候選店家審核、拒絕、合併及發布介面。
- 保存來源、授權範圍、允許欄位、更新時間與 Attribution。
- Provider 失敗、限流或配額耗盡時使用重試與熔斷，不影響公開地圖。

### 關鍵原則

- 「搜尋此區域」永遠先回傳正式資料庫結果。
- 使用者不等待外部資料蒐集完成。
- 未審核候選資料不能出現在公開地圖。
- 不直接爬取或永久保存未獲授權的平台內容。
- Google Places 僅是可能的 Provider，不是預設可永久匯入的資料來源。

### 完成條件

- 熱門但資料不足的區域能進入背景蒐集佇列。
- 相同區域不會被無限制重複排程。
- 重複店家不會直接建立多筆正式資料。
- 管理員核准前，公開 API 永遠看不到候選內容。

## 14. 跨階段測試策略

每個功能階段都需要同時補齊以下測試，不把測試全部留到最後：

### 單元測試

- 純商業規則、排序、狀態轉換及 Policy。
- 不依賴真實資料庫、Google Maps 或外部 Provider。

### 整合測試

- FastAPI + PostgreSQL/PostGIS。
- Migration、空間查詢、交易與唯一性約束。
- Redis、Celery、Object Storage 只在實際導入階段開始測試。

### Contract 測試

- FastAPI OpenAPI 規格可產生 TypeScript Client。
- 產生的 Client 必須通過 TypeScript 編譯。
- 破壞性 API 變更必須在 CI 被發現。

### E2E 測試

優先覆蓋產品主流程：

1. 管理員新增並發布店家。
2. 使用者定位、搜尋區域、縮放群聚並進入餐廳頁。
3. 使用者留言、選擇再訪狀態並新增再訪。
4. 使用者建立約飯、加入、投票及查看結果。
5. 正式成員進入聊天室，退出後失去權限。
6. 封鎖後跨功能入口一致失效。

## 15. 每階段共同完成標準

一個階段只有在以下條件全部滿足時才算完成：

- 需求文件中的對應流程可以實際操作。
- API 授權與資料驗證在後端執行。
- Migration 能從上一版安全升級。
- 單元、整合、Contract 與該階段必要的 E2E 測試通過。
- Loading、Empty、Error、Retry 狀態完整。
- 手機寬度與桌面寬度完成基本驗收。
- 不含未說明的假資料、暫時繞過權限或無期限保留的開發開關。
- README 已補充啟動方式、環境變數及新服務依賴。
- 可觀測到關鍵錯誤，不以瀏覽器 Console 作為唯一診斷方式。

## 16. 建議立即執行的第一批工作

不要同時啟動所有階段。第一批只處理階段 0 與階段 1 的前半：

1. 初始化 Git 與 monorepo 目錄。
2. 建立 Next.js、FastAPI、PostGIS 與 Docker Compose。
3. 建立 health check、Migration 及測試基線。
4. 建立 `cuisines`、`restaurants` 與空間欄位。
5. 建立最小管理員登入與店家 CRUD。
6. 建立地址定位／地圖點選／拖曳圖釘頁面。
7. 手動建立一批測試店家。
8. 通過階段 1 完成條件後，再開始公開地圖。

第一個產品驗收點不是「地圖畫面出現」，而是：

> 管理員能建立並發布一間具有正確座標、價格與主要料理分類的店家，且資料能由受測試的 API 正確讀取。

完成這個驗收點後，階段 2 與階段 3 才能在可靠資料上快速完成地圖體驗。
